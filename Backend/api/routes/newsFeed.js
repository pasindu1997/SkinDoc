const express = require('express');
const router = express.Router();
// const mongoose = require('mongoose');

router.get('/newsUpdate', () => {
//this will call an API to skin cancer news service (This will be implemented only if valid API service found)
});

module.exports = router;

