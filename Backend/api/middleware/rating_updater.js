// require('app');
// const router = express.Router();

// const Clinic = require('../models/clinicRate');

//TO UPDATE THE RATING OF A CLINIC
module.exports = (req, res) => {
    try {
        // const specificClinicId = Clinic.findById(req.params._id);
    } catch (err) {
        res.json(err);
    }

};
